// environment.example.ts
// Copy this file to environment.ts and fill in your own API key.
// environment.ts is gitignored and must never be committed.
export const environment = {
  production: false,
  apiKey: 'YOUR_GEMINI_API_KEY'
};
